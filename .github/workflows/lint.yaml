name: "CI"
on: [push, pull_request]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-python@v1
        with:
          python-version: '3.x'
      - name: Lint YAML
        run: make lint
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: textbook/git-checkout-submodule-action@2.0.0
      - uses: actions/setup-python@v1
        with:
          python-version: '3.x'
      - name: Check Configuration
        run: cp secrets.example.yaml secrets.yaml && make check
